import { RedHoverDirective } from './red-hover.directive';

describe('RedHoverDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new RedHoverDirective();
    expect(directive).toBeTruthy();
  });
});
